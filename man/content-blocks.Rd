% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool_result.R
\name{content-blocks}
\alias{content-blocks}
\alias{content_text}
\alias{content_image}
\alias{content_audio}
\alias{content_resource_link}
\alias{content_resource}
\title{Construct protocol-native MCP content blocks}
\usage{
content_text(text, annotations = list(), meta = list())

content_image(data, mime_type, annotations = list(), meta = list())

content_audio(data, mime_type, annotations = list(), meta = list())

content_resource_link(
  uri,
  name,
  title = NULL,
  description = NULL,
  mime_type = NULL,
  size = NULL,
  annotations = list(),
  meta = list()
)

content_resource(
  uri,
  text = NULL,
  blob = NULL,
  mime_type = NULL,
  annotations = list(),
  meta = list()
)
}
\arguments{
\item{text}{A single text string. For \code{content_resource()}, the resource text
payload; exactly one of \code{text} or \code{blob} must be supplied.}

\item{annotations}{Optional named MCP annotations. Known R-facing fields are
\code{audience}, \code{priority}, and \code{last_modified}; named extension fields are
preserved when serializable.}

\item{meta}{Optional named outer content-block metadata. For
\code{content_resource()}, metadata is not duplicated into the inner resource
contents object.}

\item{data}{A scalar base64 string containing inline image or audio data.}

\item{mime_type}{A single MIME type string. Optional for resources and
resource links.}

\item{uri}{A single non-empty author-supplied resource URI.}

\item{name}{A single non-empty resource-link name.}

\item{title}{Optional resource-link display title.}

\item{description}{Optional resource-link description.}

\item{size}{Optional non-negative whole-number resource size in bytes.}

\item{blob}{A scalar base64 string containing an embedded resource payload;
exactly one of \code{text} or \code{blob} must be supplied.}
}
\value{
A validated content block for use in \code{\link[=tool_result]{tool_result()}}.
}
\description{
These constructors create the only content-block objects accepted by
\code{\link[=tool_result]{tool_result()}}. All R-facing names use \code{snake_case}; MCP names such as
\code{mimeType} and \verb{_meta} are created only when a result is serialized.
}
\details{
Image, audio, and resource blob data must already be scalar base64 strings.
The constructors do not read files, download URLs, infer MIME types, encode,
decode, or validate base64 data, or invent resource URIs. Tool authors own
payload integrity, accurate MIME types, meaningful URIs, authorization,
sanitization, and payload size decisions.

Resource links only transport an author-supplied URI. \code{mcplite} remains a
tools-only server and does not make that URI readable or implement
\code{resources/read}. Embedded resources are self-contained content blocks.
}
